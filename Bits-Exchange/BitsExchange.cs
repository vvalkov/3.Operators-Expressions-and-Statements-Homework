using System;

    class BitsExchange
    {
        static void bitexchange(int count)
        {
            int p =   
        }

        static void Main()
        {
            while (true)
            {
                Console.WriteLine("The application will exchange bits 3, 4 and 5 with bits 24, 25 and 26 of a given 32-bit unsigned integer");
                Console.WriteLine("Please enter integer number");
                uint n = uint.Parse(Console.ReadLine());
                int p = 5;
                int n = 291;               
                int nRightP = n >> p;    
                int bit = nRightP & 1;
            }
        }
    }